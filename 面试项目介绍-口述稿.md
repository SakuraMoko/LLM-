# 面试项目介绍口述稿

> 目标：流畅讲 2~3 分钟  
> 场景：「我看你简历上有个 LLM 智能语音对话平台，能详细介绍一下这个项目吗？」

---

## 一、完整口述版（2~3分钟）

**开场：项目是什么**

这个项目是一个基于大语言模型的智能对话 Web 应用，  
核心功能是支持文字、语音、文件多种方式与 AI 对话，  
并且支持多轮上下文、实时流式输出和历史会话管理。  
类似 ChatGPT，但是自己完整搭建的前端应用。

---

**我负责什么**

我负责整个前端的开发，以及对接大模型 API。  
具体包括：  
页面组件开发、状态管理设计、语音输入模块、文件解析模块，  
以及流式响应的接收与实时渲染。

---

**技术栈**

技术栈主要是：  
Vue3 + Vite 负责页面构建；  
Pinia 做全局状态管理，管理消息列表和历史会话；  
Element Plus 提供 UI 组件；  
Web Speech API 实现语音输入；  
pdfjs-dist 和 mammoth 解析 PDF 和 DOCX 文件；  
vue-virtual-scroller 做虚拟列表渲染性能优化；  
后端接口用的是阿里云 DashScope 的 OpenAI 兼容模式，模型用的是通义千问。

---

**核心功能**

有三个我比较想重点介绍的功能：

第一个是**语音输入**。  
用 Web Speech API 封装了一个 `useSpeech` 的组合式函数，  
支持实时展示识别中间结果，停止后自动填入输入框，  
还做了浏览器兼容判断和错误提示处理。

第二个是**流式对话**。  
用 Fetch + ReadableStream 读取 SSE 格式的流式响应，  
每收到一个 token 片段就通过 Pinia 追加到界面，  
实现了类似 ChatGPT 那种逐字打出的效果。

第三个是**历史会话管理**。  
用 Pinia + localStorage 做了会话的持久化，  
支持多会话切换、删除、标题自动命名，  
刷新页面后历史对话仍然保留。

---

**难点和怎么解决的**

我遇到过两个比较有挑战性的问题：

**难点1：流式响应的稳定性问题。**  
SSE 数据流有时候会一次性返回多个事件行，也有时候返回不完整的 JSON，  
直接 JSON.parse 会报错导致渲染中断。  
解决方案是：把每个 chunk 按换行切割，逐行判断是否以 `data:` 开头，  
过滤掉 `[DONE]` 和空行，再对每行单独 try-catch，  
解析失败的行直接跳过，保证流式渲染不会崩溃。  
另外还加了 AbortController 支持用户随时停止生成，以及指数退避的自动重试机制。

**难点2：大量消息时的渲染性能问题。**  
当消息条数超过 50~100 条时，如果用普通 v-for 渲染，  
DOM 节点太多会造成滚动卡顿。  
解决方案是引入 vue-virtual-scroller 的 DynamicScroller，  
只渲染视口内可见的消息，其余用空白占位，  
大幅减少了 DOM 节点数量，滚动明显流畅了。

---

**结尾**

整体来说，这个项目从 API 对接、流式处理、状态设计到性能优化都是我独立完成的，  
让我对前端架构设计和 LLM 应用开发有了比较完整的实践经验。

---

## 二、精简版（1分钟，适合简短介绍）

这是一个基于通义千问大模型的智能对话 Web 应用。  
我负责全部前端开发和 API 对接。  
技术栈用的是 Vue3 + Pinia + Element Plus，  
核心功能包括语音输入、流式对话、文件解析和历史会话管理。  
主要难点是流式 SSE 响应的稳定解析和大量消息时的虚拟列表性能优化，  
这两块都做了完整的解决方案。

---

## 三、常见追问 + 回答模板

### 追问1：「流式输出具体是怎么实现的？」

用 Fetch 请求大模型接口，开启 stream 模式，  
用 ReadableStream 的 `getReader()` 逐段读取返回内容，  
按 SSE 格式解析每行 `data:` 字段，提取 `choices[0].delta.content`，  
每拿到一个 token 就追加到 Pinia 的 `streamingText` 状态，  
Vue 的响应式系统自动驱动界面实时更新。

---

### 追问2：「Pinia 里你怎么设计状态的？」

我拆了两个 store：  
`chatStore` 管当前会话的消息列表和流式状态，  
`sessionStore` 管历史会话列表，并负责 localStorage 的读写。  
这样职责分离，切换会话时只需要把 sessionStore 里的消息列表加载到 chatStore 就行了。

---

### 追问3：「Web Speech API 有什么兼容性问题？」

主要是 Firefox 不支持 `webkitSpeechRecognition`，  
所以我加了 `isSupported` 的判断，  
不支持的浏览器会有友好提示引导用户换 Chrome 或 Edge。  
同时对麦克风权限被拒绝、无语音、网络错误等场景都做了细分的错误提示。

---

### 追问4：「你提到虚拟列表，具体怎么实现的？」

用了 vue-virtual-scroller 的 DynamicScroller 组件，  
它只渲染视口范围内的消息节点，超出范围的用占位 div 撑高度，  
所以不管有多少消息，真实 DOM 节点数量始终保持在十几个左右，  
滚动性能不会随消息数量增加而下降。

---

### 追问5：「这个项目线上有地址吗？能展示一下吗？」

目前是本地开发版本，可以随时演示。  
后续计划部署到云服务器，预计接入自己的 API Key 后就可以上线。

---

## 四、关键词速记卡（背熟这些词）

| 模块 | 关键词 |
|------|--------|
| 项目定位 | 多模态 LLM 对话 Web 应用 |
| 技术栈 | Vue3 / Pinia / Vite / Element Plus / Web Speech API |
| 模型接口 | DashScope / OpenAI 兼容 / 通义千问 |
| 流式输出 | SSE / ReadableStream / delta.content / 逐 token 追加 |
| 性能优化 | vue-virtual-scroller / DynamicScroller / 虚拟列表 |
| 语音输入 | webkitSpeechRecognition / continuous / interimResults |
| 文件解析 | pdfjs-dist / mammoth / FileReader / 截断策略 |
| 持久化 | localStorage / Pinia / 多会话切换 |
| 稳定性 | AbortController / 指数退避重试 / try-catch 逐行解析 |

---

## 五、口述练习建议

1. 先背"完整口述版"，不要逐字背，用自己的话说出来
2. 对着镜子或录音，讲3遍以上
3. 故意让别人打断你问"那流式怎么实现的"，练习衔接
4. 最终目标：2分钟讲完，不卡壳
