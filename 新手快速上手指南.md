# 小白快速上手指南（AI语云项目）

> 适合对象：刚入门、几乎没写过项目、看不懂 LLM / 流式 / Pinia 等术语的人。  
> 目标：让你在 **1~3 天内** 看懂这个项目的核心，并能自己改一点功能。

---

## 0. 先给你一颗定心丸

你现在看不懂非常正常。  
这个项目里面同时用了：前端框架、状态管理、接口请求、语音识别、文件解析、流式输出……

对新手来说，**一次看不懂是正常情况，不是你能力问题**。

你只要按本文档的顺序走：
- 先知道“它是干什么的”
- 再知道“代码主要在哪几处”
- 再知道“一条消息是怎么走完流程的”

你就会突然“开窍”。

---

## 1. 这个项目到底在做什么？（一句话）

这是一个“像 ChatGPT 一样聊天”的网页：
- 你输入文字（或语音、或上传文件）
- 它把内容发给大模型
- 大模型一边生成一边显示（流式输出）
- 对话历史会保存，下次刷新还能看到

---

## 2. 你先只记住 7 个词（超级简化版）

### 1) Vue
一个写网页界面的工具（框架）。

### 2) 组件（Component）
页面上的“小零件”，比如：侧边栏、输入框、聊天窗口。

### 3) Pinia
一个“全局记忆本”，专门存数据（比如消息列表、当前会话）。

### 4) API
你前端发请求给后端/模型服务的地址。

### 5) LLM
大语言模型（比如通义千问）。你可以把它当“会聊天的 AI 引擎”。

### 6) 流式输出（Streaming）
不是等 AI 全写完再显示，而是“边生成边显示”。

### 7) localStorage
浏览器本地小仓库。刷新后数据还在，历史会话靠它保存。

---

## 3. 先别看全部代码，只看这 6 个文件

> 按顺序看，不要跳。

1. `src/App.vue`  
   看页面总结构（左边会话、右边聊天）。

2. `src/components/InputArea.vue`  
   看“你点击发送按钮后会发生什么”。

3. `src/api/llmApi.js`  
   看“请求怎么发给大模型、怎么接收流式结果”。

4. `src/stores/chatStore.js`  
   看“消息怎么存起来、流式文本怎么拼接”。

5. `src/stores/sessionStore.js`  
   看“历史会话怎么保存和切换”。

6. `src/components/ChatWindow.vue`  
   看“消息如何显示到页面”。

---

## 4. 一条消息的真实流程（最重要）

你在输入框点击发送后：

1. `InputArea.vue` 收集输入内容（文字/语音/附件）
2. 调用 `chatStore.addUserMessage()` 把你的话先显示出来
3. 调用 `chatStore.startAssistantMessage()` 告诉系统“AI开始回复了”
4. 调用 `llmApi.streamChat()` 发请求给大模型
5. 大模型返回一小段一小段文本（流式）
6. 每收到一段，就 `chatStore.appendStreamChunk()` 追加
7. 全部结束后 `chatStore.finalizeAssistantMessage()` 固化为正式消息
8. `sessionStore.saveMessages()` 保存到本地（localStorage）

你只要理解这 8 步，项目核心就懂了。

---

## 5. 新手看代码的正确方式（避免崩溃）

### 错误方式（千万别）
- 一次看完整个项目
- 看到不懂词就停住半小时
- 试图一遍就全部理解

### 正确方式（推荐）
- 每次只看 1 个文件，最多 20 分钟
- 只问自己 3 个问题：
  1) 这个文件主要干嘛？
  2) 输入是什么？
  3) 输出是什么？
- 不懂的词先“跳过”，先把流程串起来

---

## 6. 给你一套“3天上手计划”（零基础版）

## Day 1：看懂主流程（70%）

看：
- `App.vue`
- `InputArea.vue`
- `llmApi.js`

任务：
- [ ] 自己讲一遍“点击发送后发生了什么”
- [ ] 在 `handleSend()` 里加 `console.log` 看输入内容
- [ ] 在 `onChunk` 里打印流式片段

验收：
- [ ] 你能说出这3个文件分别负责什么

---

## Day 2：看懂数据保存（20%）

看：
- `chatStore.js`
- `sessionStore.js`

任务：
- [ ] 新建2个会话并切换
- [ ] 刷新页面，确认历史仍在
- [ ] 打开浏览器开发者工具查看 localStorage

验收：
- [ ] 你能解释“为什么刷新后会话还在”

---

## Day 3：看懂增强功能（10%）

看：
- `speechModule.js` + `VoiceButton.vue`
- `fileParser.js` + `FileUpload.vue`
- `ChatWindow.vue`

任务：
- [ ] 试一次语音输入
- [ ] 试一次上传 txt/pdf/docx
- [ ] 看 AI 回复是否有流式效果

验收：
- [ ] 你能说出语音和文件是怎么进入发送内容的

---

## 7. 你现在最容易卡住的点（提前帮你避坑）

### 1) 报错 401 Unauthorized
大概率是 `.env.local` 的 API Key 不对，或改完没重启 `npm run dev`。

### 2) 报错 404 Not Found
大概率是代理路径不对。检查 `vite.config.js` 是否代理到：
`https://dashscope.aliyuncs.com/compatible-mode/v1`

### 3) 语音没反应
请用 Chrome/Edge，并允许麦克风权限。

### 4) 看不懂某个函数
先看函数名 + 入参 + 返回值，不要先抠细节。

---

## 8. 项目里的“基础函数”你要先认识这些

你会频繁看到这些 JavaScript 基础：

- `const` / `let`
- `if / else`
- `for`
- 箭头函数：`const fn = () => {}`
- `async / await`（处理异步请求）
- 数组方法：`map`、`filter`、`find`、`push`
- 对象解构：`const { a, b } = obj`

如果这些还不熟，建议每天补 20 分钟基础语法，再回来看项目，会快很多。

---

## 9. 你可以立刻做的 3 个“最小练习”

### 练习1：改欢迎语
文件：`src/components/ChatWindow.vue`  
把欢迎文案改成你自己的版本。

### 练习2：改系统提示词
文件：`src/stores/chatStore.js`  
修改 system prompt，观察 AI 回答风格变化。

### 练习3：改主题色
文件：`src/assets/styles/global.css`  
修改 `--accent`，看全站颜色变化。

做完这 3 个练习，你会有“我能改动项目”的信心。

---

## 10. 给未来你的学习路线（下一步）

当你看懂本项目后，再按这个顺序提升：

1. JavaScript 基础（异步、数组、对象）
2. Vue3 组合式 API（`ref`、`computed`、`watch`）
3. Pinia 状态管理
4. 前端请求与错误处理
5. LLM API 调用与流式返回

---

## 11. 一句话结尾

> 你现在不需要“全懂”，你只需要先把**一条消息的流程**看懂，项目就已经入门成功了。

如果你愿意，我下一步可以继续给你做一份：
**《这个项目逐行注释版（先从 InputArea.vue 开始）》**，我会按“这行代码是干嘛的”方式带你读。
